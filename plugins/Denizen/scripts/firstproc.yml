firstproc:
  type: procedure
  debug: false
  definitions: value_1|value_2
  script:
  - determine "%value_1% is the 1st value and the 2nd value in the list %value_2%"
  #/ex narrate <proc[firstproc].context[<player.location.formatted>|<player.name>]>
  #/ex narrate <proc[firstproc].context[li@1|2]>
  
AttributeMod_attackDamage:
  type: procedure
  debug: true
  script:
  - determine "<player.item_in_hand.nbt[AttributeModifiers].after[<&lc>generic.attackDamage,].before[<&rc>]||>"
  # /ex narrate <proc[AttributeMod_attackDamage].context[<player>]>